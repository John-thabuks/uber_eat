from models import db, Rider#, Owner, Customer, Meal, Restaurant, Review, Payment, Order
from faker import Faker
import random
# import string

# fake = Faker()

# alphabet_letters = list(string.ascii_uppercase)

# for i in range(10):
#     name = fake.name
#     byke_type =fake.words
#     plate_no = f"KMT{random.choice(alphabet_letters)}{random.choice(alphabet_letters)} {random.randint(100, 999)}{random.choice(alphabet_letters)}"
#     user_id =

#     new_rider = Rider(userz.name = name, bike_type=byke_type, plate_number=plate_no, user_id=)
#     db.session.add(new_rider)


# db.session.commit()

